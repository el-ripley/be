"""
Helpers for building OpenAI-ready message context:

- Image processing utilities
- Message conversion (DB → OpenAI payloads)
- High-level context building for a conversation branch via `ContextBuilder`

These modules are intended to be used directly (e.g. via
`ContextBuilder`) rather than hidden behind a compatibility facade.
"""

__all__ = []
